<%= form_for(@menu, html: {class: 'form-horizontal', role: 'form'}) do |f| %>
<% if @menu.errors.any? %>
<div id="error_explanation">
	<h2><%= pluralize(@menu.errors.count, "error") %> forhindrede denne menu fra at blive vist:</h2>
	<ul>
		<% @menu.errors.full_messages.each do |msg| %>
		<li><%= msg %></li>
		<% end %>
	</ul>
</div>
<% end %>

<div class="form-group">
	<%= f.label :navn, class: 'col-sm-2 control-label' %>
	<div class="col-sm-10">	
		<%= f.text_field :name, class: 'form-control' %>
	</div>
</div>
<div class="form-group">
	<%= f.label :titel, class: 'col-sm-2 control-label' %>
	<div class="col-sm-10">	
		<%= f.text_field :title, class: 'form-control' %>
	</div>
</div>
<div class="form-group">
	<%= f.label :aktiv, class: 'col-sm-2 control-label' %>
	<div class="col-sm-10">	
		<%= f.check_box :active, class: 'form-control' %>
	</div>
</div>

<div class='form-group'>
	<div class="actions col-sm-offset-2 col-sm-10">
		<%= f.submit('Opdater', class: 'btn btn-info btn-xs') %>
	</div>
</div>
<% end %>