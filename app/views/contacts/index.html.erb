<div class="container">

	<h1>Viser kontakter</h1>
	<div class="btn-group index">
		<%= link_to 'Ny kontakt', new_contact_path, class: 'btn btn-default btn-xs' %>
	</div>
	<div class="table-responsive">
		<table class="table table-striped table-hover">
			<thead>
				<tr>
					<th class="">Navn</th>
					<th class="visible-lg visible-md visible-sm">Beskrivelse</th>
					<th class="">Telefon</th>
					<th class="visible-lg visible-md visible-sm">Email</th>
					<th class="visible-lg visible-md visible-sm">Partner</th>
					<th colspan="3"></th>
				</tr>
			</thead>

			<tbody>
				<% @contacts.each do |contact| %>
					<tr>
						<td class=""><%= contact.name %></td>
						<td class="visible-lg visible-md visible-sm"><%= contact.description %></td>
						<td class=""><%= contact.phone %></td>
						<td class="visible-lg visible-md visible-sm"><%= contact.email %></td>
						<td class="visible-lg visible-md visible-sm"><%= contact.partner_id %></td>
						<td colspan="3" class="text-right">
							<div class="btn-group">
								<%= link_to 'Ret', edit_contact_path(contact), class: 'btn btn-default btn-xs' %>
								<%= link_to 'Slet', contact, method: :delete, data: { confirm: 'Er du sikker?' }, class: 'btn btn-default btn-xs' %>
							</div>
						</td>
					</tr>
				<% end %>
			</tbody>
		</table>
	</div>
</div>