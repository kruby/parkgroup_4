<div class="container">
	
	<p id="notice"><%= notice %></p>

	<p>
		<strong>Navn:</strong>
		<%= @page.name %>
	</p>

	<p>
		<strong>Titel:</strong>
		<%= @page.title %>
	</p>

	<p>
		<strong>Brødtekst:</strong>
		<%= @page.body %>
	</p>

	<p>
		<strong>Overskrift:</strong>
		<%= @page.headline %>
	</p>

	<p>
		<strong>Aktiv:</strong>
		<%= @page.active %>
	</p>


	<%= link_to 'Ret', edit_page_path(@page) %>
	<%= link_to 'Tilbage', pages_path %>

</div>